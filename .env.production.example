# Copied to .env.production on the VPS — never committed with real values
# DATABASE_URL password must match POSTGRES_PASSWORD
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql+asyncpg://dmv2:change-me@db:5432/dmv2
SECRET_KEY=change-me
